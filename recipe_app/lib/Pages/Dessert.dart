import 'package:flutter/material.dart';

class Dessert extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Center(child: Image.asset('images/dessert.jpeg', fit: BoxFit.fitWidth));
  }
}
